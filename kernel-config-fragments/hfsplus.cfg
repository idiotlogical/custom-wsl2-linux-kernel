# Kernel config fragment to enable HFS+ support
# Add this to your kernel source `.config` or use the helper scripts to apply it.
# Set HFS+ built into the kernel. Change to `m` if you prefer a module.
CONFIG_HFSPLUS_FS=y
